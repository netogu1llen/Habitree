<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Habitree Control Center - Login</title>
    <link rel="icon" type="image/png" href="/img/LogoHabitree.png">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/login.css">
</head>
<body>

    <div class="login-container">
        <div class="left-section">
            <h1>Habitree Control Center</h1>
            <p>Manage the entire platform from a central location. Add missions, manage users, and keep the ecosystem running smoothly.</p>
        </div>
        
        <div class="right-section">
            <h2 class="welcome-title">Welcome Back!</h2>
            
            <form action="/login" method="POST">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>" >
                 <% if (failed) { %>
                    <div class="error-message"><%= failed %></div>
                <% } %>
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" placeholder="user@gmail.com" required>
                </div>
                
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" placeholder="••••••••••" required>
                </div>
                
                <button type="submit" class="login-btn">Log in</button>
                
            </form>
        </div>
    </div>
</body>
</html>